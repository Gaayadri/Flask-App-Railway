services:
  - type: web
    name: flask-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"  # Change 'app' to your main Flask filename (without .py)
    pythonVersion: 3.10
